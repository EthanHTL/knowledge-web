# Comparator 接口

```java
// 任何比较器：
// compare方法里，遵循一个统一的规范：
// 返回负数的时候，认为第一个参数应该排在前面
// 返回正数的时候，认为第二个参数应该排在前面
// 返回0的时候，认为无所谓谁放前面

/**
 * 总结：
 * public int compare(A,B) {
 * 		return A-B;
 * }
 * 当 A-B 时，升序  小->大
 * 当 B-A 时，降序  大->小
 */
```

